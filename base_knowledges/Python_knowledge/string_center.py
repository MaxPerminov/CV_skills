# The center() method will center align the string.
# Parameters:
# length	- Required. The length of the returned string
# character	- Optional. The character to fill the indents. Default is " " (space)

a = "asd"
print(a.center(70, "-"))
